// Load the main SCSS style sheet
import '../styles/style.scss'

if(module.hot) {
    module.hot.accept()
}


// Initial test to see if Webpack is working
// Delete when it does work
console.log('Webpack just ran!')